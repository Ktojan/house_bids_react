/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",  // <=== enables static exports
  basePath: "/house_bids_react",
  reactStrictMode: true,
};

export default nextConfig;
